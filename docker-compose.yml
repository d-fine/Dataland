services:
  backend:
    build:
      dockerfile: ./dataland-backend/Dockerfile
    ports:
      - "8080:8080"
  e2etests:
    build:
      dockerfile: ./dataland-e2etests/Dockerfile
    ports:
      - "8081:8080"