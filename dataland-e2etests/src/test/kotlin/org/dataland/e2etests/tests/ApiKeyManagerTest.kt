package org.dataland.e2etests.tests

import org.junit.jupiter.api.Assertions.assertEquals
import org.junit.jupiter.api.Test

class ApiKeyManagerTest {
/*
    private val apiAccessor = ApiAccessor()

    private val apiKeyManagerClient = "a"

    private val testDataEuTaxonomyNonFinancials = apiAccessor.testDataProviderForEuTaxonomyDataForNonFinancials
        .getTData(1).first()

    private val testCompanyInformation = apiAccessor.testDataProviderForEuTaxonomyDataForNonFinancials
        .getCompanyInformationWithoutIdentifiers(1).first()


    private fun someFunction(
    )
    {
        println("test")
    }
*/
    @Test
    fun `Retrieve an API key and get a non-teaser company with it`() {
        // val companyAssociatedDataEuTaxonomyDataForNonFinancials = "a"
        assertEquals(
            "a",
            "a",
            "Error message."
        )
    }
}
