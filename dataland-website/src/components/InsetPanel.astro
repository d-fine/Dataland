---
interface Props {
  title: string;
  class?: string;
}

const { title, class: className } = Astro.props;
---

<section
  class:list={[
    'flex flex-col gap-4 rounded-xl border border-[var(--color-surface-200)] bg-[var(--color-surface-50)] p-6 text-[var(--color-text)]',
    className,
  ]}
>
  <h3 class="m-0 text-[1.14rem] font-bold">{title}</h3>
  <slot />
</section>
