// <--WARNING--> THIS FILE IS AUTO-GENERATED BY THE FRAMEWORK-TOOLBOX AND WILL BE OVERWRITTEN
import fs from 'fs';
import { generateNuclearAndGasFixtures } from '@e2e/fixtures/frameworks/nuclear-and-gas/NuclearAndGasDataFixtures';
import { generateNuclearAndGasPreparedFixtures } from '@e2e/fixtures/frameworks/nuclear-and-gas/NuclearAndGasPreparedFixtures';
import { FAKE_FIXTURES_PER_FRAMEWORK } from '@e2e/fixtures/GenerateFakeFixtures';

/**
 * Generates and exports fake fixtures for the nuclear-and-gas framework
 */
function exportFixturesNuclearAndGas(): void {
  const companyInformationWithNuclearAndGasData = generateNuclearAndGasFixtures(FAKE_FIXTURES_PER_FRAMEWORK);
  fs.writeFileSync(
    '../testing/data/CompanyInformationWithNuclearAndGasData.json',
    JSON.stringify(companyInformationWithNuclearAndGasData, null, '\t')
  );
  const preparedFixtureNuclearAndGasData = generateNuclearAndGasPreparedFixtures();
  fs.writeFileSync(
    '../testing/data/CompanyInformationWithNuclearAndGasPreparedFixtures.json',
    JSON.stringify(preparedFixtureNuclearAndGasData, null, '\t')
  );
}

export default exportFixturesNuclearAndGas;
