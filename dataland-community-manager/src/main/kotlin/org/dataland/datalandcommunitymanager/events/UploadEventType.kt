package org.dataland.datalandcommunitymanager.events

/**
 * Enum class for events related to uploads (of datasets, documents, etc.). Currently, only data uploads are included.
 */
enum class UploadEventType {
    DataUploadEvent,
}
